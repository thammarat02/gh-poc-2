name: Cloudflare Purge Cache

on:
  workflow_dispatch:

jobs:
  test-tag-release:
    name: Cloudflare Purge Cache
    runs-on: ubuntu-latest
    steps:
      - name: Purge cache
        uses: jakejarvis/cloudflare-purge-action@master
        env:
          CLOUDFLARE_ZONE: 48ce70a7db64058d68aebee881bfcfb1
          CLOUDFLARE_TOKEN: S5AbCut3ilSyWzDC6Bw9FhsG2o28NSS--_THM6ug
